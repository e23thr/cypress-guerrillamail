version: "2.1"
orbs:
  cypress: cypress-io/cypress@5.0.1
workflows:
  test:
    jobs:
      - cypress/run:
          filters:
            branches:
              only:
                - release
          install-command: npm install
          start-command: npm test
